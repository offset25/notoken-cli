{
  "intents": [
    {
      "name": "service.restart",
      "description": "Restart a service in an environment",
      "synonyms": [
        "restart",
        "bounce",
        "reload",
        "recycle",
        "kick"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": true,
          "default": "dev"
        }
      },
      "command": "sudo systemctl restart {{service}} && sudo systemctl status {{service}} --no-pager",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "allowlist": [
        "nginx",
        "redis",
        "api",
        "worker",
        "postgres"
      ],
      "examples": [
        "restart nginx on prod",
        "bounce redis in production",
        "restart api service in staging"
      ]
    },
    {
      "name": "service.status",
      "description": "Check the status of a service",
      "synonyms": [
        "status",
        "check service",
        "is running",
        "service health"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo systemctl status {{service}} --no-pager",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "allowlist": [
        "nginx",
        "redis",
        "api",
        "worker",
        "postgres"
      ],
      "examples": [
        "status of nginx on prod",
        "is redis running on staging",
        "check api service health"
      ]
    },
    {
      "name": "server.check_disk",
      "description": "Check disk usage on a target server or specific path",
      "synonyms": [
        "disk",
        "disk usage",
        "space",
        "storage",
        "df",
        "check disk",
        "disk space",
        "how much space",
        "free space",
        "space on",
        "check space"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "df -h && echo '\\nTop directories:\\n' && du -sh /var/* 2>/dev/null | sort -rh | head -10",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "check disk usage on prod",
        "show disk space on api server",
        "how much space on my documents",
        "check space on c drive",
        "disk space on /home",
        "free space on downloads",
        "what is eating space on staging"
      ]
    },
    {
      "name": "server.check_memory",
      "description": "Check memory usage on a target server",
      "synonyms": [
        "memory",
        "ram",
        "mem",
        "free",
        "htop",
        "memory usage",
        "memory free"
      ],
      "fields": {
        "target": {
          "type": "service",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "free -h && echo '\\nTop 15 processes by memory:\\n' && ps aux --sort=-%mem | head -16 | awk '{printf \"%-10s %-8s %-6s %-6s %s\\n\", $1, $2, $4\"%\", $6/1024\"MB\", $11}'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "check memory on prod",
        "how much ram is used on staging",
        "show memory usage"
      ]
    },
    {
      "name": "server.uptime",
      "description": "Check server uptime, load, and top processes with analysis",
      "synonyms": [
        "uptime",
        "load",
        "load average",
        "check load",
        "server load",
        "server seems sluggish",
        "server seems slow",
        "figure out why",
        "how long has the server been running",
        "how long has this been running",
        "what is the load",
        "what is the load on my server",
        "check server load",
        "server status",
        "is the server ok",
        "is the server healthy",
        "system load",
        "how busy is the server",
        "server health",
        "check my server"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "local"
        }
      },
      "command": "echo '=== Uptime ===' && uptime && echo '' && echo '=== CPUs ===' && nproc && echo '' && echo '=== Load ===' && cat /proc/loadavg 2>/dev/null && echo '' && echo '=== Memory ===' && free -h 2>/dev/null && echo '' && echo '=== Top Processes (by CPU) ===' && ps aux --sort=-%cpu | head -8 && echo '' && echo '=== Top Processes (by Memory) ===' && ps aux --sort=-%mem | head -6",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "uptime on prod",
        "show load average on staging",
        "how long has prod been up",
        "what is the load on my server",
        "how long has the server been running",
        "check server health"
      ]
    },
    {
      "name": "logs.find",
      "description": "Find and list log files on the system, optionally for a specific service",
      "synonyms": [
        "find logs",
        "find log files",
        "list log files",
        "where are the logs",
        "log files",
        "show log files",
        "what logs"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "echo '=== /var/log ===' && ls -lhS /var/log/*.log /var/log/*/*.log 2>/dev/null | head -20 && echo '' && echo '=== Large logs (>50MB) ===' && find /var/log -type f -size +50M -exec ls -lh {} \\; 2>/dev/null && echo '' && echo '=== Recently modified ===' && find /var/log -type f -mmin -60 -name '*.log' 2>/dev/null | head -10",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "find log files on prod",
        "where are the logs on staging",
        "list log files on prod",
        "what logs are on the server",
        "find logs for nginx on prod"
      ]
    },
    {
      "name": "logs.errors",
      "description": "Search for errors across common log files",
      "synonyms": [
        "check logs for errors",
        "error logs",
        "show errors",
        "recent errors",
        "any errors",
        "check for errors"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "echo '=== Syslog errors ===' && grep -i 'error\\|fail\\|critical' /var/log/syslog 2>/dev/null | tail -10 || grep -i 'error\\|fail\\|critical' /var/log/messages 2>/dev/null | tail -10 && echo '' && echo '=== Auth errors ===' && grep -i 'error\\|fail' /var/log/auth.log 2>/dev/null | tail -5 || grep -i 'error\\|fail' /var/log/secure 2>/dev/null | tail -5 && echo '' && echo '=== Nginx errors ===' && tail -10 /var/log/nginx/error.log 2>/dev/null || echo 'No nginx error log' && echo '' && echo '=== Kernel errors ===' && dmesg --level=err,crit,alert,emerg 2>/dev/null | tail -5",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "check logs for errors on prod",
        "any errors on staging",
        "show recent errors on prod",
        "error logs on staging"
      ]
    },
    {
      "name": "logs.check",
      "description": "Check a specific log file — shows recent entries, size, and rotation status",
      "synonyms": [
        "check log",
        "check log file",
        "check the log",
        "look at log"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": true
        },
        "lines": {
          "type": "number",
          "required": false,
          "default": 30
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "echo '=== File info ===' && ls -lh {{path}} && echo '' && echo '=== Last {{lines}} lines ===' && tail -{{lines}} {{path}} && echo '' && echo '=== Rotation ===' && ls -lh {{path}}* 2>/dev/null | head -5",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "check /var/log/syslog on prod",
        "check the nginx error log on staging",
        "look at /var/log/auth.log on prod"
      ]
    },
    {
      "name": "logs.tail",
      "description": "Tail logs for a service",
      "synonyms": [
        "tail",
        "logs",
        "log",
        "watch logs",
        "show logs"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": false,
          "default": "api"
        },
        "lines": {
          "type": "number",
          "required": false,
          "default": 100
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "tail -n {{lines}} {{logPath}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "logPaths": {
        "nginx": "/var/log/nginx/error.log",
        "api": "/var/log/api/app.log",
        "redis": "/var/log/redis/redis-server.log",
        "worker": "/var/log/worker/worker.log",
        "postgres": "/var/log/postgresql/postgresql.log"
      },
      "examples": [
        "tail nginx logs on prod",
        "show me the last 100 lines for api logs",
        "watch redis logs in staging"
      ]
    },
    {
      "name": "logs.search",
      "description": "Search logs for a pattern",
      "synonyms": [
        "search logs",
        "grep",
        "find in logs",
        "search for"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": false,
          "default": "api"
        },
        "query": {
          "type": "string",
          "required": false,
          "default": "error"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "grep -i '{{query}}' {{logPath}} | tail -50",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "logPaths": {
        "nginx": "/var/log/nginx/error.log",
        "api": "/var/log/api/app.log",
        "redis": "/var/log/redis/redis-server.log",
        "worker": "/var/log/worker/worker.log",
        "postgres": "/var/log/postgresql/postgresql.log"
      },
      "examples": [
        "search for errors in api logs",
        "find timeout in nginx logs on prod",
        "grep 500 in api logs"
      ]
    },
    {
      "name": "deploy.run",
      "description": "Run a deployment",
      "synonyms": [
        "deploy",
        "release",
        "ship",
        "push",
        "promote"
      ],
      "fields": {
        "app": {
          "type": "string",
          "required": false
        },
        "branch": {
          "type": "branch",
          "required": false,
          "default": "main"
        },
        "environment": {
          "type": "environment",
          "required": true,
          "default": "staging"
        }
      },
      "command": "cd /srv/myapp && git fetch origin && git checkout {{branch}} && git pull origin {{branch}} && ./deploy.sh",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "deploy latest to staging",
        "deploy main to prod",
        "push release branch to production"
      ]
    },
    {
      "name": "deploy.rollback",
      "description": "Rollback a deployment",
      "synonyms": [
        "rollback",
        "revert",
        "undo deploy",
        "roll back",
        "revert deploy",
        "revert last deploy"
      ],
      "fields": {
        "app": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": true,
          "default": "staging"
        }
      },
      "command": "cd /srv/myapp && ./rollback.sh",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "rollback deploy on prod",
        "revert last deploy in staging",
        "undo production deploy"
      ]
    },
    {
      "name": "docker.list",
      "description": "List running Docker containers",
      "synonyms": [
        "docker ps",
        "containers",
        "list containers",
        "show containers",
        "docker list"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker ps --format 'table {{.Names}}\\t{{.Status}}\\t{{.Ports}}'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show docker containers on prod",
        "list containers in staging",
        "what is running in docker"
      ]
    },
    {
      "name": "docker.restart",
      "description": "Restart a Docker container",
      "synonyms": [
        "restart container",
        "docker restart",
        "bounce container"
      ],
      "fields": {
        "container": {
          "type": "service",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker restart {{container}} && docker ps -f name={{container}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "allowlist": [
        "nginx",
        "redis",
        "api",
        "worker",
        "postgres"
      ],
      "examples": [
        "restart docker container api on prod",
        "bounce the redis container in staging"
      ]
    },
    {
      "name": "docker.stop",
      "description": "Stop a running Docker container",
      "synonyms": [
        "docker stop",
        "stop container",
        "stop docker"
      ],
      "fields": {
        "container": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker stop {{container}} && echo 'Stopped: {{container}}'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "stop container api on prod",
        "docker stop redis on staging"
      ]
    },
    {
      "name": "docker.start",
      "description": "Start a stopped Docker container",
      "synonyms": [
        "docker start",
        "start container"
      ],
      "fields": {
        "container": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker start {{container}} && docker ps -f name={{container}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "start container api on prod",
        "docker start redis on staging"
      ]
    },
    {
      "name": "docker.logs",
      "description": "View logs for a Docker container",
      "synonyms": [
        "docker logs",
        "container logs",
        "show container logs"
      ],
      "fields": {
        "container": {
          "type": "string",
          "required": true
        },
        "lines": {
          "type": "number",
          "required": false,
          "default": 50
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker logs --tail {{lines}} {{container}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "docker logs api on prod",
        "show container logs for redis",
        "docker logs nginx 100 on staging"
      ]
    },
    {
      "name": "docker.all",
      "description": "List all containers including stopped ones",
      "synonyms": [
        "all containers",
        "docker ps -a",
        "stopped containers",
        "all docker",
        "show all containers"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker ps -a --format 'table {{.Names}}\\t{{.Status}}\\t{{.Image}}\\t{{.Size}}'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show all containers on prod",
        "stopped containers on staging",
        "docker ps -a"
      ]
    },
    {
      "name": "docker.images",
      "description": "List Docker images",
      "synonyms": [
        "docker images",
        "list images",
        "show images",
        "docker image list"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker images --format 'table {{.Repository}}\\t{{.Tag}}\\t{{.Size}}\\t{{.CreatedSince}}'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "docker images on prod",
        "list images on staging"
      ]
    },
    {
      "name": "docker.cleanup",
      "description": "Clean up unused/orphaned Docker resources — stopped containers, dangling images, unused volumes and networks",
      "synonyms": [
        "docker cleanup",
        "docker prune",
        "clean docker",
        "orphaned containers",
        "docker garbage",
        "docker disk",
        "docker space",
        "unused containers",
        "unused images",
        "dangling images"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "echo '=== Stopped containers ===' && docker ps -a --filter status=exited --format 'table {{.Names}}\\t{{.Status}}\\t{{.Size}}' && echo '' && echo '=== Dangling images ===' && docker images -f dangling=true --format 'table {{.Repository}}\\t{{.Tag}}\\t{{.Size}}' && echo '' && echo '=== Disk usage ===' && docker system df && echo '' && echo 'Run \"docker system prune -a\" to clean up (will ask for confirmation)'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "docker cleanup on prod",
        "check orphaned containers on staging",
        "docker disk usage",
        "unused containers on prod",
        "dangling images on staging"
      ]
    },
    {
      "name": "docker.prune",
      "description": "Remove all stopped containers, unused images, networks, and volumes",
      "synonyms": [
        "docker system prune",
        "prune docker",
        "remove unused docker",
        "free docker space"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker system prune -af --volumes && echo '' && docker system df",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "docker system prune on prod",
        "prune docker on staging",
        "free docker space on prod"
      ]
    },
    {
      "name": "docker.compose_up",
      "description": "Start services with docker compose",
      "synonyms": [
        "docker compose up",
        "compose up",
        "docker-compose up",
        "start compose",
        "run compose"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "service": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker compose -f {{path}}/docker-compose.yml up -d {{service}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "docker compose up on prod",
        "compose up on staging",
        "start compose on dev",
        "docker compose up api on prod"
      ]
    },
    {
      "name": "docker.compose_down",
      "description": "Stop and remove docker compose services",
      "synonyms": [
        "docker compose down",
        "compose down",
        "docker-compose down",
        "stop compose",
        "tear down compose"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker compose -f {{path}}/docker-compose.yml down",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "docker compose down on prod",
        "compose down on staging",
        "stop compose on dev"
      ]
    },
    {
      "name": "docker.compose_build",
      "description": "Build or rebuild docker compose services",
      "synonyms": [
        "docker compose build",
        "compose build",
        "docker-compose build",
        "rebuild compose",
        "rebuild containers",
        "docker rebuild"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "service": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker compose -f {{path}}/docker-compose.yml build --no-cache {{service}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "docker compose build on prod",
        "rebuild compose on staging",
        "rebuild containers on dev",
        "docker rebuild api on prod"
      ]
    },
    {
      "name": "docker.compose_status",
      "description": "Show status of docker compose services",
      "synonyms": [
        "docker compose status",
        "compose status",
        "docker compose ps",
        "compose ps"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker compose -f {{path}}/docker-compose.yml ps",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "docker compose status on prod",
        "compose ps on staging"
      ]
    },
    {
      "name": "docker.compose_restart",
      "description": "Restart docker compose services",
      "synonyms": [
        "docker compose restart",
        "compose restart",
        "restart compose"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "service": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "docker compose -f {{path}}/docker-compose.yml restart {{service}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "docker compose restart on prod",
        "restart compose on staging",
        "compose restart api on prod"
      ]
    },
    {
      "name": "cron.list",
      "description": "List all cron jobs — user crontab, system crons, and timers",
      "synonyms": [
        "cron",
        "crontab",
        "cron jobs",
        "scheduled tasks",
        "list cron",
        "check my crontabs",
        "what crontabs are running",
        "show my cron jobs",
        "what crons do i have",
        "check crontab",
        "show scheduled tasks",
        "what is scheduled",
        "my crontabs",
        "list crontabs",
        "cron list",
        "show crons"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "local"
        }
      },
      "command": "echo '=== User Crontab ===' && crontab -l 2>/dev/null || echo '(no user crontab)' && echo '' && echo '=== System Crons (/etc/cron.d/) ===' && ls -la /etc/cron.d/ 2>/dev/null || echo '(none)' && echo '' && echo '=== Cron Directories ===' && echo 'Hourly:' && ls /etc/cron.hourly/ 2>/dev/null || echo '  (empty)' && echo 'Daily:' && ls /etc/cron.daily/ 2>/dev/null || echo '  (empty)' && echo 'Weekly:' && ls /etc/cron.weekly/ 2>/dev/null || echo '  (empty)' && echo 'Monthly:' && ls /etc/cron.monthly/ 2>/dev/null || echo '  (empty)' && echo '' && echo '=== Systemd Timers ===' && systemctl list-timers --no-pager 2>/dev/null | head -20 || echo '(no systemd timers)'",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show cron jobs on prod",
        "list scheduled tasks on staging",
        "what crons are running",
        "check my crontabs",
        "what crontabs do i have"
      ]
    },
    {
      "name": "network.ports",
      "description": "Show listening ports on a server",
      "synonyms": [
        "ports",
        "listening",
        "netstat",
        "open ports",
        "ss"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ss -tlnp | head -30",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show open ports on prod",
        "what ports are listening on staging",
        "netstat on prod"
      ]
    },
    {
      "name": "network.ping",
      "description": "Ping a host or check connectivity",
      "synonyms": [
        "ping",
        "check connectivity",
        "is reachable"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ping -c 4 {{target}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "ping prod",
        "check connectivity to staging"
      ]
    },
    {
      "name": "files.find",
      "description": "Find files or directories by name or pattern",
      "synonyms": [
        "find file",
        "find files",
        "find log",
        "locate",
        "find directory",
        "where is",
        "search file",
        "find config"
      ],
      "fields": {
        "pattern": {
          "type": "string",
          "required": false,
          "default": "*"
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "/"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "find {{path}} -name '{{pattern}}' -maxdepth 5 2>/dev/null | head -30",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "find log files on prod",
        "locate nginx config on staging",
        "find files named *.conf on prod"
      ]
    },
    {
      "name": "files.find_media",
      "description": "Find media files — videos, images, music, documents",
      "synonyms": [
        "find media", "find my media", "media files", "where are my media files",
        "find movies", "find my movies", "where are my movies",
        "find videos", "find my videos", "where are my videos",
        "find images", "find my images", "find my photos", "where are my photos", "find photos", "find pictures",
        "find music", "find my music", "where is my music", "find audio",
        "find documents", "find my documents", "where are my documents",
        "find pdfs", "find my pdfs"
      ],
      "fields": {
        "type": { "type": "string", "required": false, "default": "all" },
        "path": { "type": "string", "required": false, "default": "." },
        "environment": { "type": "environment", "required": false, "default": "local" }
      },
      "command": "echo '=== Media Files ===' && find {{path}} -maxdepth 4 -type f \\( -iname '*.mp4' -o -iname '*.mkv' -o -iname '*.avi' -o -iname '*.mov' -o -iname '*.wmv' -o -iname '*.flv' -o -iname '*.webm' -o -iname '*.mp3' -o -iname '*.flac' -o -iname '*.wav' -o -iname '*.aac' -o -iname '*.ogg' -o -iname '*.m4a' -o -iname '*.jpg' -o -iname '*.jpeg' -o -iname '*.png' -o -iname '*.gif' -o -iname '*.bmp' -o -iname '*.webp' -o -iname '*.svg' -o -iname '*.tiff' -o -iname '*.pdf' -o -iname '*.doc' -o -iname '*.docx' -o -iname '*.ppt' -o -iname '*.pptx' -o -iname '*.xls' -o -iname '*.xlsx' \\) -not -path '*/node_modules/*' -not -path '*/.git/*' 2>/dev/null | sort && echo '' && echo '=== Summary ===' && echo 'Videos:' && find {{path}} -maxdepth 4 -type f \\( -iname '*.mp4' -o -iname '*.mkv' -o -iname '*.avi' -o -iname '*.mov' -o -iname '*.webm' \\) -not -path '*/node_modules/*' 2>/dev/null | wc -l && echo 'Images:' && find {{path}} -maxdepth 4 -type f \\( -iname '*.jpg' -o -iname '*.jpeg' -o -iname '*.png' -o -iname '*.gif' -o -iname '*.webp' -o -iname '*.svg' \\) -not -path '*/node_modules/*' 2>/dev/null | wc -l && echo 'Audio:' && find {{path}} -maxdepth 4 -type f \\( -iname '*.mp3' -o -iname '*.flac' -o -iname '*.wav' -o -iname '*.aac' -o -iname '*.ogg' \\) -not -path '*/node_modules/*' 2>/dev/null | wc -l && echo 'Documents:' && find {{path}} -maxdepth 4 -type f \\( -iname '*.pdf' -o -iname '*.doc' -o -iname '*.docx' -o -iname '*.ppt' -o -iname '*.pptx' -o -iname '*.xls' -o -iname '*.xlsx' \\) -not -path '*/node_modules/*' 2>/dev/null | wc -l",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "where are my media files",
        "find my movies",
        "find my photos",
        "where are my videos",
        "find images in /home",
        "find my documents"
      ]
    },
    {
      "name": "files.send",
      "description": "Archive a folder and send it to a remote server",
      "synonyms": [
        "send to remote", "send to server", "zip and send", "tar and send",
        "zip up and send", "archive and send", "send folder", "send project",
        "zip up my project and send", "zip up my project folder and send",
        "send files to", "transfer to", "upload to", "push to server",
        "copy folder to remote", "send directory"
      ],
      "fields": {
        "source": { "type": "string", "required": true },
        "destination": { "type": "string", "required": false, "default": "/root" },
        "environment": { "type": "environment", "required": false, "default": "dev" }
      },
      "command": "ARCHIVE=$(basename {{source}})-$(date +%Y%m%d-%H%M%S).tar.gz && tar -czf /tmp/$ARCHIVE -C $(dirname {{source}}) $(basename {{source}}) && echo \"Created /tmp/$ARCHIVE ($(du -sh /tmp/$ARCHIVE | cut -f1))\" && scp /tmp/$ARCHIVE {{environment}}:{{destination}}/$ARCHIVE && echo \"Sent to {{environment}}:{{destination}}/$ARCHIVE\" && rm /tmp/$ARCHIVE",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "zip up my project folder and send it to prod /root",
        "send /srv/myapp to staging /backup",
        "archive and send this folder to dev",
        "tar and send /var/www to prod /backup"
      ]
    },
    {
      "name": "files.grep",
      "description": "Search file contents for a pattern",
      "synonyms": [
        "grep",
        "search for text",
        "find text",
        "search in files",
        "look for"
      ],
      "fields": {
        "query": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "/var/log"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "grep -rn '{{query}}' {{path}} 2>/dev/null | head -30",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "grep for error in /var/log on prod",
        "search for timeout in api logs on staging",
        "find text connection refused on prod"
      ]
    },
    {
      "name": "files.list",
      "description": "List files and directories",
      "synonyms": [
        "list files",
        "ls",
        "show files",
        "directory listing",
        "what files",
        "do an ls",
        "what is in this folder",
        "what's here",
        "show directory"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ls -lah {{path}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "list files in /etc/nginx on prod",
        "ls /var/log on staging",
        "show files in /srv on prod"
      ]
    },
    {
      "name": "files.tail",
      "description": "Tail any file by path",
      "synonyms": [
        "tail file",
        "last lines",
        "end of file",
        "tail -f"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": true
        },
        "lines": {
          "type": "number",
          "required": false,
          "default": 50
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "tail -n {{lines}} {{path}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "tail /var/log/syslog on prod",
        "show last 200 lines of /tmp/output.log on staging"
      ]
    },
    {
      "name": "process.list",
      "description": "List running processes, optionally filtered",
      "synonyms": [
        "ps",
        "processes",
        "show processes",
        "what is running",
        "top processes",
        "process list"
      ],
      "fields": {
        "filter": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ps aux --sort=-%cpu | head -20",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show processes on prod",
        "ps on staging",
        "what is running on prod",
        "top processes on staging"
      ]
    },
    {
      "name": "process.kill",
      "description": "Kill a process by name or PID",
      "synonyms": [
        "kill process",
        "stop process",
        "kill pid",
        "terminate"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "pkill -f '{{target}}' && echo 'Killed processes matching: {{target}}'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "kill process node on staging",
        "stop process java on dev",
        "terminate worker on prod"
      ]
    },
    {
      "name": "ssh.connect",
      "description": "Show SSH connection command for an environment",
      "synonyms": [
        "ssh",
        "connect",
        "ssh into",
        "shell into",
        "login to"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": true,
          "default": "dev"
        }
      },
      "command": "echo 'Connected to {{environment}}' && hostname && whoami && uptime",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "ssh into prod",
        "connect to staging",
        "login to dev"
      ]
    },
    {
      "name": "user.add",
      "description": "Create a new system user",
      "synonyms": [
        "useradd",
        "add user",
        "create user",
        "new user"
      ],
      "fields": {
        "username": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": true,
          "default": "dev"
        }
      },
      "command": "sudo useradd -m -s /bin/bash {{username}} && echo 'User {{username}} created'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "create user deploy on prod",
        "add user monitoring on staging",
        "useradd backup on prod"
      ]
    },
    {
      "name": "user.modify",
      "description": "Modify an existing system user",
      "synonyms": [
        "usermod",
        "modify user",
        "change user",
        "update user",
        "add to group",
        "add user to group",
        "user to group",
        "to group docker",
        "to group sudo",
        "to group www-data",
        "to group admin"
      ],
      "fields": {
        "username": {
          "type": "string",
          "required": true
        },
        "group": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": true,
          "default": "dev"
        }
      },
      "command": "sudo usermod -aG {{group}} {{username}} && echo 'User {{username}} updated' && id {{username}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "add user deploy to group docker on prod",
        "modify user monitoring add to sudo on staging",
        "usermod deploy add to www-data on prod"
      ]
    },
    {
      "name": "files.copy",
      "description": "Copy a file or directory to a destination",
      "synonyms": [
        "copy file",
        "copy this",
        "cp",
        "copy to",
        "duplicate file",
        "copy over",
        "copy"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "cp -rv {{source}} {{destination}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "fuzzyResolve": [
        "source"
      ],
      "examples": [
        "copy nginx.conf to /root on prod",
        "copy the api config to /tmp on staging",
        "cp app.log to /backup on prod"
      ]
    },
    {
      "name": "files.move",
      "description": "Move or rename a file or directory",
      "synonyms": [
        "move file",
        "move this",
        "move to",
        "rename file",
        "move over",
        "move"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "mv -v {{source}} {{destination}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "fuzzyResolve": [
        "source"
      ],
      "examples": [
        "move app.log to /root on prod",
        "move this config to /backup on staging",
        "rename the old log on prod"
      ]
    },
    {
      "name": "files.remove",
      "description": "Remove a file (not directories)",
      "synonyms": [
        "delete file",
        "remove file",
        "rm",
        "delete this"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "rm -v {{target}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "fuzzyResolve": [
        "target"
      ],
      "examples": [
        "delete old.log on prod",
        "remove temp file on staging"
      ]
    },
    {
      "name": "user.list",
      "description": "List system users or show user info",
      "synonyms": [
        "list users",
        "show users",
        "who",
        "whoami",
        "user info",
        "id",
        "users history the one we are running as"
      ],
      "fields": {
        "username": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "cat /etc/passwd | grep -v nologin | grep -v false | cut -d: -f1,3,6",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "list users on prod",
        "show users on staging",
        "who is on prod"
      ]
    },
    {
      "name": "git.status",
      "description": "Show git working tree status",
      "synonyms": [
        "git status",
        "git state",
        "what changed",
        "uncommitted changes",
        "working tree"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} status",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "git status",
        "show git status",
        "what changed in the repo",
        "uncommitted changes"
      ]
    },
    {
      "name": "git.log",
      "description": "Show recent git commit history",
      "synonyms": [
        "git log",
        "commit history",
        "recent commits",
        "git history",
        "show commits",
        "last commits",
        "show last",
        "last 5 commits",
        "last 10 commits"
      ],
      "fields": {
        "count": {
          "type": "number",
          "required": false,
          "default": 10
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} log --oneline -n {{count}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "git log",
        "show last 20 commits",
        "recent commit history",
        "git log 5"
      ]
    },
    {
      "name": "git.diff",
      "description": "Show git diff of current changes",
      "synonyms": [
        "git diff",
        "show diff",
        "what is different",
        "show changes"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "target": {
          "type": "string",
          "required": false
        }
      },
      "command": "git -C {{path}} diff {{target}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "git diff",
        "show diff",
        "what is different",
        "git diff main"
      ]
    },
    {
      "name": "git.pull",
      "description": "Pull latest changes from remote",
      "synonyms": [
        "git pull",
        "pull latest",
        "pull changes",
        "update repo",
        "fetch and merge"
      ],
      "fields": {
        "remote": {
          "type": "string",
          "required": false,
          "default": "origin"
        },
        "branch": {
          "type": "branch",
          "required": false
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} pull {{remote}} {{branch}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "git pull",
        "pull latest changes",
        "update the repo",
        "git pull origin main"
      ]
    },
    {
      "name": "git.push",
      "description": "Push commits to remote",
      "synonyms": [
        "git push",
        "push commits",
        "push to remote",
        "push changes"
      ],
      "fields": {
        "remote": {
          "type": "string",
          "required": false,
          "default": "origin"
        },
        "branch": {
          "type": "branch",
          "required": false
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} push {{remote}} {{branch}}",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "git push",
        "push commits to origin",
        "push to remote",
        "git push origin main"
      ]
    },
    {
      "name": "git.branch",
      "description": "List, create, or switch git branches",
      "synonyms": [
        "git branch",
        "list branches",
        "show branches",
        "branches",
        "current branch"
      ],
      "fields": {
        "name": {
          "type": "string",
          "required": false
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} branch -a",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "git branch",
        "list branches",
        "show all branches",
        "what branch am I on"
      ]
    },
    {
      "name": "git.checkout",
      "description": "Switch to a different branch",
      "synonyms": [
        "git checkout",
        "switch branch",
        "switch to",
        "checkout branch",
        "go to branch"
      ],
      "fields": {
        "branch": {
          "type": "branch",
          "required": true
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} checkout {{branch}}",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "git checkout main",
        "switch to develop",
        "checkout branch main",
        "go to branch develop"
      ]
    },
    {
      "name": "git.commit",
      "description": "Commit staged changes with a message",
      "synonyms": [
        "git commit",
        "commit changes",
        "save changes",
        "commit with message"
      ],
      "fields": {
        "message": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} commit -m '{{message}}'",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "git commit fix typo",
        "commit changes update readme",
        "commit with message initial setup"
      ]
    },
    {
      "name": "git.add",
      "description": "Stage files for commit",
      "synonyms": [
        "git add",
        "stage files",
        "stage changes",
        "add files",
        "add to staging",
        "stage all",
        "stage all files"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} add {{target}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "git add all",
        "stage all files",
        "git add src/index.ts",
        "stage changes"
      ]
    },
    {
      "name": "git.stash",
      "description": "Stash or restore uncommitted changes",
      "synonyms": [
        "git stash",
        "stash changes",
        "save work",
        "stash it",
        "stash pop",
        "restore stash"
      ],
      "fields": {
        "action": {
          "type": "string",
          "required": false,
          "default": "push"
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} stash {{action}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "git stash",
        "stash my changes",
        "restore stash",
        "git stash pop"
      ]
    },
    {
      "name": "git.reset",
      "description": "Reset staged changes or undo last commit",
      "synonyms": [
        "git reset",
        "unstage",
        "undo commit",
        "reset head"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": false,
          "default": "HEAD"
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "."
        }
      },
      "command": "git -C {{path}} reset {{target}}",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "git reset",
        "unstage all files",
        "undo last commit",
        "git reset HEAD~1"
      ]
    },
    {
      "name": "file.parse",
      "description": "Read and parse a config file (passwd, shadow, .env, yaml, json, nginx, apache)",
      "synonyms": [
        "parse file",
        "parse /",
        "read file",
        "show config",
        "open file",
        "read config",
        "parse config",
        "show file contents",
        "cat file",
        "parse",
        "read /",
        "cat /"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "cat {{path}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "parse /etc/passwd",
        "read the nginx config",
        "show .env file",
        "open /etc/shadow",
        "read docker-compose.yml"
      ]
    },
    {
      "name": "file.read",
      "description": "Read a file with smart output — shows sample if file is large",
      "synonyms": [
        "show me",
        "output file",
        "print file",
        "display file",
        "cat",
        "show file",
        "show the file",
        "look at",
        "open the"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "cat {{path}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show me package.json",
        "output /etc/nginx/nginx.conf",
        "look at the README",
        "show the .env file",
        "cat tsconfig.json"
      ]
    },
    {
      "name": "file.search_in",
      "description": "Search for something specific inside a file and show matches with context",
      "synonyms": [
        "search in file",
        "find in file",
        "look for in",
        "show me the part",
        "where does it say",
        "find where",
        "search file for"
      ],
      "fields": {
        "query": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "grep -n -i -C 3 '{{query}}' {{path}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "search for proxy_pass in /etc/nginx/nginx.conf",
        "find database in .env",
        "where does it say listen in nginx.conf",
        "show me the part about ssl in the config",
        "find where port is defined in package.json"
      ]
    },
    {
      "name": "file.search",
      "description": "Find where a config or service file is located",
      "synonyms": [
        "where is",
        "find config",
        "find file for",
        "locate config",
        "show me where",
        "where are the"
      ],
      "fields": {
        "query": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "echo 'Searching for: {{query}}'",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "where is the nginx config",
        "find config for postgres",
        "where are the ssh keys",
        "locate redis config",
        "find the .env file"
      ]
    },
    {
      "name": "env.get",
      "description": "Read a variable from a .env file",
      "synonyms": [
        "get env",
        "show env",
        "read env",
        "what is the value of",
        "env value",
        "read the .env",
        "show the .env",
        "read .env",
        "read the .env file"
      ],
      "fields": {
        "key": {
          "type": "string",
          "required": false
        },
        "path": {
          "type": "string",
          "required": false,
          "default": ".env"
        }
      },
      "command": "cat {{path}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "get env DATABASE_URL",
        "show env API_KEY from .env.production",
        "what is the value of REDIS_HOST",
        "read env file"
      ]
    },
    {
      "name": "env.set",
      "description": "Save a variable to a .env file with smart naming",
      "synonyms": [
        "set env",
        "save env",
        "save variable",
        "add env",
        "add variable",
        "set variable",
        "save to env",
        "save database",
        "save password",
        "save api key",
        "save token"
      ],
      "fields": {
        "key": {
          "type": "string",
          "required": false
        },
        "value": {
          "type": "string",
          "required": true
        },
        "topic": {
          "type": "string",
          "required": false
        },
        "purpose": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false
        },
        "path": {
          "type": "string",
          "required": false,
          "default": ".env"
        }
      },
      "command": "echo 'Setting env variable'",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "save database password for staging to .env",
        "set env API_KEY=abc123",
        "add redis url for prod",
        "save variable smtp password"
      ]
    },
    {
      "name": "archive.tar",
      "description": "Create a tar.gz archive from files or directories",
      "synonyms": [
        "tar",
        "tar.gz",
        "tarball",
        "create tar",
        "create archive",
        "compress",
        "tar up",
        "make archive",
        "archive folder",
        "archive directory",
        "compress folder",
        "compress directory",
        "tar this folder",
        "tar my project"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "tar -czvf {{destination}} {{source}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "tar /var/log/nginx to /tmp/nginx-logs.tar.gz",
        "create tar.gz of /etc/nginx on prod",
        "archive /srv/myapp to /backup/app.tar.gz",
        "compress /var/log on staging",
        "tar up the nginx config on prod"
      ]
    },
    {
      "name": "archive.untar",
      "description": "Extract a tar.gz archive",
      "synonyms": [
        "untar",
        "extract tar",
        "extract archive",
        "decompress tar",
        "untar.gz",
        "tar extract",
        "unpack tar",
        "unarchive",
        "extract .tar",
        "extract .gz",
        "untar /",
        "extract /"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "tar -xzvf {{source}} -C {{destination}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "fuzzyResolve": [
        "source"
      ],
      "examples": [
        "untar /tmp/backup.tar.gz to /srv/restore",
        "extract archive /backup/app.tar.gz on prod",
        "decompress /tmp/logs.tar.gz on staging",
        "unpack tar /root/data.tar.gz"
      ]
    },
    {
      "name": "archive.zip",
      "description": "Create a zip archive from files or directories",
      "synonyms": [
        "zip",
        "create zip",
        "zip up",
        "make zip",
        "zip folder",
        "zip directory",
        "zip up folder",
        "zip up my project",
        "zip up my project folder",
        "zip this folder",
        "zip my files"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "zip -r {{destination}} {{source}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "zip /var/log/nginx to /tmp/nginx-logs.zip",
        "create zip of /etc/nginx on prod",
        "zip up /srv/myapp on staging"
      ]
    },
    {
      "name": "archive.unzip",
      "description": "Extract a zip archive",
      "synonyms": [
        "unzip",
        "extract zip",
        "decompress zip",
        "unpack zip"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false,
          "default": "."
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "unzip -o {{source}} -d {{destination}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "fuzzyResolve": [
        "source"
      ],
      "examples": [
        "unzip /tmp/backup.zip to /srv/restore",
        "extract zip /root/data.zip on prod",
        "unpack zip /tmp/files.zip on staging"
      ]
    },
    {
      "name": "archive.list",
      "description": "List contents of a tar.gz or zip archive without extracting",
      "synonyms": [
        "list archive",
        "show archive",
        "what is in the archive",
        "list tar",
        "list zip",
        "archive contents"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "if echo '{{source}}' | grep -qE '\\.zip$'; then unzip -l {{source}}; else tar -tzvf {{source}}; fi",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "fuzzyResolve": [
        "source"
      ],
      "examples": [
        "list archive /tmp/backup.tar.gz",
        "what is in /root/data.zip",
        "show archive contents of /backup/app.tar.gz on prod"
      ]
    },
    {
      "name": "sync.rsync",
      "description": "Sync files between locations using rsync",
      "synonyms": [
        "rsync",
        "sync files",
        "sync to",
        "sync from",
        "mirror files",
        "rsync to",
        "rsync from"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "rsync -avz --progress {{source}} {{destination}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "rsync /var/www to /backup/www on prod",
        "sync /srv/myapp to user@backup-server:/backup/",
        "rsync from /opt/data to /mnt/nas on staging",
        "mirror files /etc/nginx to /backup/nginx-config"
      ]
    },
    {
      "name": "sync.rsync_remote",
      "description": "Rsync files between two servers or from local to remote",
      "synonyms": [
        "rsync remote",
        "sync remote",
        "rsync between",
        "sync between servers",
        "push files to",
        "pull files from"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false
        }
      },
      "command": "rsync -avz --progress -e ssh {{source}} {{destination}}",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "push files /var/www to user@prod:/var/www",
        "pull files from staging:/var/log to /tmp/logs",
        "rsync between dev and staging for /srv/app"
      ]
    },
    {
      "name": "backup.create",
      "description": "Create a backup of files, directories, or databases",
      "synonyms": [
        "back up",
        "create backup",
        "make backup",
        "snapshot",
        "backup to",
        "backup /"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false,
          "default": "/backup"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "DEST={{destination}}/$(basename {{source}})-$(date +%Y%m%d-%H%M%S).tar.gz && tar -czvf $DEST {{source}} && echo 'Backup created: '$DEST",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "backup /etc/nginx on prod",
        "create backup of /srv/myapp to /backup on staging",
        "back up the database config on prod",
        "backup /var/www on prod"
      ]
    },
    {
      "name": "backup.restore",
      "description": "Restore from a backup archive",
      "synonyms": [
        "restore backup",
        "restore from backup",
        "recover",
        "restore from"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false,
          "default": "/"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "tar -xzvf {{source}} -C {{destination}} && echo 'Restored from: {{source}}'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "fuzzyResolve": [
        "source"
      ],
      "examples": [
        "restore backup /backup/nginx-20240101.tar.gz on prod",
        "restore from /backup/app-latest.tar.gz to /srv/myapp",
        "recover from backup on staging"
      ]
    },
    {
      "name": "backup.list",
      "description": "List available backups",
      "synonyms": [
        "list backups",
        "show backups",
        "available backups",
        "what backups"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "/backup"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ls -lah {{path}}/*.tar.gz {{path}}/*.zip 2>/dev/null | sort -k6,7 || echo 'No backups found in {{path}}'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "list backups on prod",
        "show available backups on staging",
        "what backups are in /backup on prod"
      ]
    },
    {
      "name": "package.install",
      "description": "Install a system package",
      "synonyms": [
        "install package",
        "apt install",
        "yum install",
        "dnf install",
        "install"
      ],
      "fields": {
        "package": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "if command -v apt-get >/dev/null; then sudo apt-get install -y {{package}}; elif command -v dnf >/dev/null; then sudo dnf install -y {{package}}; elif command -v yum >/dev/null; then sudo yum install -y {{package}}; else echo 'No supported package manager found'; fi",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "install nginx on prod",
        "apt install certbot on staging",
        "install whois on dev",
        "install zip on prod"
      ]
    },
    {
      "name": "package.check",
      "description": "Check if a package is installed",
      "synonyms": [
        "is installed",
        "check package",
        "which",
        "package installed",
        "command exists",
        "is zip installed",
        "is nginx installed",
        "is certbot installed"
      ],
      "fields": {
        "package": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "command -v {{package}} && echo '{{package}} is installed' && {{package}} --version 2>/dev/null || dpkg -l {{package}} 2>/dev/null || rpm -q {{package}} 2>/dev/null || echo '{{package}} is NOT installed'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "is nginx installed on prod",
        "check package certbot on staging",
        "which rsync on dev",
        "is zip installed"
      ]
    },
    {
      "name": "swap.create",
      "description": "Create and enable a swap file",
      "synonyms": [
        "create swap",
        "add swap",
        "make swap",
        "enable swap",
        "swapfile"
      ],
      "fields": {
        "size": {
          "type": "string",
          "required": false,
          "default": "2G"
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "/swapfile"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo fallocate -l {{size}} {{path}} && sudo chmod 600 {{path}} && sudo mkswap {{path}} && sudo swapon {{path}} && echo '{{path}} ({{size}}) activated' && free -h",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "create swap 4G on prod",
        "add swap file on staging",
        "make 2G swapfile on dev"
      ]
    },
    {
      "name": "swap.delete",
      "description": "Disable and remove a swap file",
      "synonyms": [
        "delete swap",
        "remove swap",
        "disable swap",
        "remove swapfile"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": false,
          "default": "/swapfile"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo swapoff {{path}} && sudo rm -f {{path}} && echo 'Swap removed: {{path}}' && free -h",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "delete swap on prod",
        "remove swapfile on staging",
        "disable swap on dev"
      ]
    },
    {
      "name": "swap.status",
      "description": "Show current swap usage",
      "synonyms": [
        "swap status",
        "show swap",
        "swap usage",
        "swapon"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "swapon --show && echo '' && free -h",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show swap on prod",
        "swap status on staging",
        "swap usage on dev"
      ]
    },
    {
      "name": "network.curl",
      "description": "Make an HTTP request to a URL",
      "synonyms": [
        "curl",
        "http request",
        "fetch url",
        "get url",
        "hit endpoint",
        "call api",
        "wget"
      ],
      "fields": {
        "url": {
          "type": "string",
          "required": true
        },
        "method": {
          "type": "string",
          "required": false,
          "default": "GET"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "curl -sS -X {{method}} -w '\\n\\nHTTP %{http_code} | %{time_total}s | %{size_download} bytes' {{url}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "curl https://example.com",
        "hit endpoint https://api.example.com/health on prod",
        "fetch url https://example.com/api/status",
        "call api https://localhost:3000/health on dev"
      ]
    },
    {
      "name": "network.whois",
      "description": "Look up domain registration and DNS information",
      "synonyms": [
        "whois",
        "domain info",
        "domain lookup",
        "who owns",
        "domain registration",
        "rdap"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "whois {{target}} 2>/dev/null || curl -sS 'https://rdap.org/domain/{{target}}' | head -50",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "whois example.com",
        "who owns google.com",
        "domain info example.org",
        "domain lookup mysite.io"
      ]
    },
    {
      "name": "network.dig",
      "description": "DNS lookup for a domain",
      "synonyms": [
        "dig",
        "dns lookup",
        "nslookup",
        "resolve dns",
        "dns records",
        "lookup dns"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": true
        },
        "recordType": {
          "type": "string",
          "required": false,
          "default": "A"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "dig {{target}} {{recordType}} +short && echo '\\n--- Full ---\\n' && dig {{target}} {{recordType}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "dig example.com",
        "dns lookup google.com",
        "nslookup example.com MX",
        "resolve dns example.org",
        "dns records for mysite.io"
      ]
    },
    {
      "name": "network.traceroute",
      "description": "Trace the network route to a host",
      "synonyms": [
        "traceroute",
        "trace route",
        "tracepath",
        "trace network"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "traceroute -m 15 {{target}} 2>/dev/null || tracepath {{target}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "traceroute google.com",
        "trace route to 8.8.8.8 on prod",
        "tracepath example.com"
      ]
    },
    {
      "name": "perm.check",
      "description": "Check file or directory permissions and access",
      "synonyms": [
        "check permissions",
        "file permissions",
        "who can access",
        "can i read",
        "can i write",
        "access check",
        "stat file",
        "show permissions"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "stat -c '%A %a %U %G %s %n' {{path}} && echo '' && ls -la {{path}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "check permissions on /etc/shadow",
        "who can access /var/www on prod",
        "show permissions for /etc/nginx on staging",
        "can I write to /var/log on prod"
      ]
    },
    {
      "name": "perm.chmod",
      "description": "Change file or directory permissions",
      "synonyms": [
        "chmod",
        "change permissions",
        "set permissions",
        "make executable",
        "make readable",
        "make writable",
        "set read only",
        "secure file"
      ],
      "fields": {
        "mode": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": true
        },
        "recursive": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo chmod {{mode}} {{path}} && stat -c '%A %a %U %G %n' {{path}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "chmod 755 /var/www on prod",
        "make /deploy.sh executable on staging",
        "set 600 on /etc/ssl/private/key.pem",
        "change permissions to 644 on /etc/nginx/nginx.conf",
        "secure /root/.ssh/id_rsa on prod"
      ]
    },
    {
      "name": "perm.chown",
      "description": "Change file or directory ownership",
      "synonyms": [
        "chown",
        "change owner",
        "change ownership",
        "set owner",
        "give to user",
        "give ownership"
      ],
      "fields": {
        "owner": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo chown {{owner}} {{path}} && stat -c '%A %a %U %G %n' {{path}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "chown www-data:www-data /var/www on prod",
        "change owner of /srv/app to deploy on staging",
        "give /var/log/app to deploy:deploy on prod"
      ]
    },
    {
      "name": "perm.chown_recursive",
      "description": "Recursively change ownership of a directory",
      "synonyms": [
        "chown recursive",
        "change owner recursively",
        "chown -R"
      ],
      "fields": {
        "owner": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo chown -R {{owner}} {{path}} && echo 'Ownership changed recursively' && ls -la {{path}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "chown -R www-data:www-data /var/www on prod",
        "change owner recursively deploy:deploy /srv/app on staging"
      ]
    },
    {
      "name": "perm.chmod_recursive",
      "description": "Recursively change permissions of a directory",
      "synonyms": [
        "chmod recursive",
        "chmod -R",
        "change permissions recursively"
      ],
      "fields": {
        "mode": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo chmod -R {{mode}} {{path}} && echo 'Permissions changed recursively' && ls -la {{path}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "chmod -R 755 /var/www on prod",
        "change permissions recursively 644 /etc/nginx on staging"
      ]
    },
    {
      "name": "system.history",
      "description": "Show shell command history",
      "synonyms": [
        "history",
        "command history",
        "bash history",
        "check history",
        "show history",
        "recent commands",
        "what did i run",
        "show command history"
      ],
      "fields": {
        "query": {
          "type": "string",
          "required": false
        },
        "lines": {
          "type": "number",
          "required": false,
          "default": 30
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "if [ -n '{{query}}' ]; then history 2>/dev/null | grep -i '{{query}}' | tail -{{lines}} || cat ~/.bash_history 2>/dev/null | grep -i '{{query}}' | tail -{{lines}}; else history 2>/dev/null | tail -{{lines}} || cat ~/.bash_history 2>/dev/null | tail -{{lines}}; fi",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "check history",
        "show command history",
        "what did i run recently",
        "search history for nginx",
        "bash history on prod"
      ]
    },
    {
      "name": "system.hostname",
      "description": "Show or set the system hostname",
      "synonyms": [
        "hostname",
        "show hostname",
        "what is the hostname",
        "set hostname"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "hostname && hostnamectl 2>/dev/null || echo ''",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "hostname on prod",
        "what is the hostname on staging"
      ]
    },
    {
      "name": "system.datetime",
      "description": "Show date, time, and timezone",
      "synonyms": [
        "date",
        "time",
        "datetime",
        "timedatectl",
        "what time",
        "timezone"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "date && echo '' && timedatectl 2>/dev/null || echo ''",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "date on prod",
        "what time is it on staging",
        "show timezone"
      ]
    },
    {
      "name": "system.reboot",
      "description": "Reboot the server",
      "synonyms": [
        "reboot",
        "restart server",
        "reboot server"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": true
        }
      },
      "command": "sudo reboot",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "reboot prod",
        "restart server on staging"
      ]
    },
    {
      "name": "system.shutdown",
      "description": "Shutdown the server",
      "synonyms": [
        "shutdown",
        "power off",
        "halt server",
        "shutdown server"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": true
        }
      },
      "command": "sudo shutdown -h now",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "shutdown prod",
        "power off staging"
      ]
    },
    {
      "name": "system.info",
      "description": "Show full system information (kernel, distro, hardware)",
      "synonyms": [
        "system info",
        "uname",
        "os info",
        "kernel info",
        "distro info",
        "lsb_release",
        "neofetch",
        "what os",
        "system health",
        "os are we running"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "uname -a && echo '' && cat /etc/os-release 2>/dev/null && echo '' && lscpu | head -15 2>/dev/null",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "system info on prod",
        "uname on staging",
        "what distro is prod running"
      ]
    },
    {
      "name": "systemd.enable",
      "description": "Enable a service to start on boot",
      "synonyms": [
        "systemctl enable",
        "enable service",
        "enable on boot",
        "auto start",
        "enable",
        "enable nginx on boot",
        "enable redis on boot",
        "enable api on boot"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo systemctl enable {{service}} && systemctl is-enabled {{service}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "enable nginx on boot on prod",
        "systemctl enable redis on staging"
      ]
    },
    {
      "name": "systemd.disable",
      "description": "Disable a service from starting on boot",
      "synonyms": [
        "systemctl disable",
        "disable service",
        "disable on boot",
        "no auto start"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo systemctl disable {{service}} && systemctl is-enabled {{service}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "disable nginx on boot on prod",
        "systemctl disable redis on staging"
      ]
    },
    {
      "name": "systemd.start",
      "description": "Start a service",
      "synonyms": [
        "start service",
        "systemctl start",
        "service start"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo systemctl start {{service}} && systemctl status {{service}} --no-pager",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "start nginx on prod",
        "start redis on staging"
      ]
    },
    {
      "name": "systemd.stop",
      "description": "Stop a service",
      "synonyms": [
        "stop service",
        "systemctl stop",
        "service stop",
        "stop",
        "stop redis",
        "stop nginx",
        "stop api"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo systemctl stop {{service}} && systemctl status {{service}} --no-pager",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "stop nginx on prod",
        "stop redis on staging"
      ]
    },
    {
      "name": "logs.journal",
      "description": "View systemd journal logs",
      "synonyms": [
        "journalctl",
        "journal logs",
        "systemd logs",
        "service logs"
      ],
      "fields": {
        "service": {
          "type": "service",
          "required": false
        },
        "lines": {
          "type": "number",
          "required": false,
          "default": 50
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "journalctl -u {{service}} -n {{lines}} --no-pager 2>/dev/null || journalctl -n {{lines}} --no-pager",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "journalctl nginx on prod",
        "service logs for api on staging",
        "journal logs 100 on prod"
      ]
    },
    {
      "name": "logs.dmesg",
      "description": "View kernel messages",
      "synonyms": [
        "dmesg",
        "kernel messages",
        "kernel log",
        "boot messages"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "dmesg --time-format iso | tail -50",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "dmesg on prod",
        "kernel messages on staging"
      ]
    },
    {
      "name": "disk.mount",
      "description": "Mount a filesystem",
      "synonyms": [
        "mount",
        "mount disk",
        "mount volume",
        "mount filesystem"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo mount {{source}} {{destination}} && df -h {{destination}}",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "mount /dev/sdb1 to /mnt/data on prod"
      ]
    },
    {
      "name": "disk.unmount",
      "description": "Unmount a filesystem",
      "synonyms": [
        "unmount",
        "umount",
        "unmount disk",
        "eject"
      ],
      "fields": {
        "path": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo umount {{path}} && echo 'Unmounted {{path}}'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "unmount /mnt/data on prod",
        "umount /mnt/backup on staging"
      ]
    },
    {
      "name": "disk.list",
      "description": "List block devices and partitions",
      "synonyms": [
        "lsblk",
        "list disks",
        "show disks",
        "list partitions",
        "disk info",
        "fdisk -l",
        "show drives"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "lsblk -f && echo '' && df -h",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "list disks on prod",
        "lsblk on staging",
        "show partitions on prod"
      ]
    },
    {
      "name": "cron.add",
      "description": "Add a cron job",
      "synonyms": [
        "add cron",
        "create cron",
        "schedule job",
        "add crontab",
        "new cron"
      ],
      "fields": {
        "schedule": {
          "type": "string",
          "required": true
        },
        "command": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "(crontab -l 2>/dev/null; echo '{{schedule}} {{command}}') | crontab - && echo 'Cron added' && crontab -l",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "add cron job every hour to run /scripts/backup.sh on prod"
      ]
    },
    {
      "name": "cron.remove",
      "description": "Remove a cron job",
      "synonyms": [
        "remove cron",
        "delete cron",
        "remove crontab entry"
      ],
      "fields": {
        "pattern": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "crontab -l | grep -v '{{pattern}}' | crontab - && echo 'Removed entries matching: {{pattern}}' && crontab -l",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "remove cron job for backup.sh on prod"
      ]
    },
    {
      "name": "security.ufw",
      "description": "Manage UFW firewall rules",
      "synonyms": [
        "ufw",
        "firewall",
        "ufw status",
        "firewall status",
        "ufw allow",
        "ufw deny",
        "open port",
        "close port",
        "block port"
      ],
      "fields": {
        "action": {
          "type": "string",
          "required": false,
          "default": "status"
        },
        "port": {
          "type": "string",
          "required": false
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo ufw {{action}} {{port}} 2>/dev/null && sudo ufw status verbose || echo 'ufw not installed'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "examples": [
        "ufw status on prod",
        "open port 8080 on staging",
        "firewall status on prod",
        "block port 3306 on prod"
      ]
    },
    {
      "name": "security.fail2ban",
      "description": "Check or manage fail2ban",
      "synonyms": [
        "fail2ban",
        "fail2ban status",
        "banned ips",
        "fail2ban jails"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo fail2ban-client status 2>/dev/null && echo '' && sudo fail2ban-client status sshd 2>/dev/null || echo 'fail2ban not installed'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "fail2ban status on prod",
        "show banned ips on staging"
      ]
    },
    {
      "name": "security.iptables",
      "description": "View iptables firewall rules",
      "synonyms": [
        "iptables",
        "iptables rules",
        "show firewall rules",
        "nftables"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo iptables -L -n -v --line-numbers 2>/dev/null || sudo nft list ruleset 2>/dev/null || echo 'No iptables/nftables'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show iptables rules on prod",
        "iptables on staging"
      ]
    },
    {
      "name": "security.sshkey",
      "description": "Manage SSH keys (generate, list, add authorized)",
      "synonyms": [
        "ssh key",
        "ssh keygen",
        "generate ssh key",
        "authorized keys",
        "ssh keys",
        "list ssh keys"
      ],
      "fields": {
        "action": {
          "type": "string",
          "required": false,
          "default": "list"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "echo 'Authorized keys:' && cat ~/.ssh/authorized_keys 2>/dev/null | wc -l && echo 'keys' && echo '' && ls -la ~/.ssh/ 2>/dev/null",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "list ssh keys on prod",
        "show authorized keys on staging"
      ]
    },
    {
      "name": "cert.check",
      "description": "Check an SSL certificate for a domain or file",
      "synonyms": [
        "check certificate",
        "check ssl",
        "ssl cert",
        "cert info",
        "certificate expiry",
        "openssl check",
        "cert check"
      ],
      "fields": {
        "target": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "echo | openssl s_client -connect {{target}}:443 -servername {{target}} 2>/dev/null | openssl x509 -noout -subject -issuer -dates -fingerprint 2>/dev/null || openssl x509 -in {{target}} -noout -subject -issuer -dates 2>/dev/null || echo 'Could not check certificate'",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "check ssl for example.com",
        "cert info google.com on prod",
        "certificate expiry for mysite.io"
      ]
    },
    {
      "name": "cert.generate",
      "description": "Generate a self-signed SSL certificate",
      "synonyms": [
        "generate certificate",
        "create ssl cert",
        "self signed cert",
        "openssl genrsa",
        "generate ssl"
      ],
      "fields": {
        "domain": {
          "type": "string",
          "required": true
        },
        "path": {
          "type": "string",
          "required": false,
          "default": "/etc/ssl"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout {{path}}/private/{{domain}}.key -out {{path}}/certs/{{domain}}.crt -subj '/CN={{domain}}' && echo 'Certificate generated for {{domain}}'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "generate ssl cert for example.com on prod",
        "create self signed cert for mysite.io"
      ]
    },
    {
      "name": "db.pg_dump",
      "description": "Backup a PostgreSQL database",
      "synonyms": [
        "pg_dump",
        "dump postgres",
        "backup database",
        "backup postgres",
        "postgres dump",
        "export database"
      ],
      "fields": {
        "database": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false,
          "default": "/backup"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "pg_dump {{database}} | gzip > {{destination}}/{{database}}-$(date +%Y%m%d-%H%M%S).sql.gz && echo 'Dump complete'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "dump postgres mydb on prod",
        "backup database myapp to /backup on staging"
      ]
    },
    {
      "name": "db.pg_restore",
      "description": "Restore a PostgreSQL database from dump",
      "synonyms": [
        "pg_restore",
        "restore postgres",
        "restore database",
        "import database",
        "load database"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "database": {
          "type": "string",
          "required": true
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "gunzip -c {{source}} | psql {{database}} && echo 'Restore complete'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "high",
      "fuzzyResolve": [
        "source"
      ],
      "examples": [
        "restore postgres mydb from /backup/mydb-dump.sql.gz on staging"
      ]
    },
    {
      "name": "db.mysql_dump",
      "description": "Backup a MySQL/MariaDB database",
      "synonyms": [
        "mysqldump",
        "dump mysql",
        "backup mysql",
        "mysql dump",
        "export mysql"
      ],
      "fields": {
        "database": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": false,
          "default": "/backup"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "mysqldump {{database}} | gzip > {{destination}}/{{database}}-$(date +%Y%m%d-%H%M%S).sql.gz && echo 'Dump complete'",
      "execution": "remote",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "dump mysql mydb on prod",
        "backup mysql database on staging"
      ]
    },
    {
      "name": "db.redis_cli",
      "description": "Run Redis CLI commands",
      "synonyms": [
        "redis-cli",
        "redis cli",
        "redis command",
        "redis info",
        "redis ping"
      ],
      "fields": {
        "command": {
          "type": "string",
          "required": false,
          "default": "info"
        },
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "redis-cli {{command}}",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "redis info on prod",
        "redis-cli ping on staging",
        "redis cli dbsize on prod"
      ]
    },
    {
      "name": "network.ip",
      "description": "Show network interfaces and IP addresses",
      "synonyms": [
        "ip addr",
        "ifconfig",
        "show ip",
        "ip address",
        "network interfaces",
        "my ip",
        "what is my ip"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ip addr show 2>/dev/null || ifconfig 2>/dev/null",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show ip on prod",
        "ip addr on staging",
        "what is my ip on prod"
      ]
    },
    {
      "name": "network.route",
      "description": "Show routing table",
      "synonyms": [
        "route",
        "routing table",
        "ip route",
        "show routes",
        "netstat -r"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ip route show 2>/dev/null || route -n 2>/dev/null",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show routes on prod",
        "routing table on staging"
      ]
    },
    {
      "name": "network.connections",
      "description": "Show active network connections",
      "synonyms": [
        "connections",
        "active connections",
        "netstat",
        "established connections",
        "who is connected"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "ss -tunap | head -30 2>/dev/null || netstat -tunap | head -30",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "show connections on prod",
        "active connections on staging",
        "who is connected to prod"
      ]
    },
    {
      "name": "network.scp",
      "description": "Securely copy files between hosts",
      "synonyms": [
        "scp",
        "secure copy",
        "scp to",
        "scp from",
        "copy to remote",
        "copy from remote"
      ],
      "fields": {
        "source": {
          "type": "string",
          "required": true
        },
        "destination": {
          "type": "string",
          "required": true
        }
      },
      "command": "scp -r {{source}} {{destination}}",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": [
        "scp /tmp/backup.tar.gz to user@prod:/backup/",
        "secure copy config.yml to staging:/etc/app/"
      ]
    },
    {
      "name": "network.bandwidth",
      "description": "Check network bandwidth and usage",
      "synonyms": [
        "bandwidth",
        "network usage",
        "nethogs",
        "iftop",
        "network speed"
      ],
      "fields": {
        "environment": {
          "type": "environment",
          "required": false,
          "default": "dev"
        }
      },
      "command": "cat /proc/net/dev && echo '' && ip -s link 2>/dev/null | head -30",
      "execution": "remote",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "bandwidth on prod",
        "network usage on staging"
      ]
    },
    {
      "name": "llm.ask",
      "description": "Ask an LLM (Claude or ChatGPT) a question",
      "synonyms": [
        "ask claude",
        "ask chatgpt",
        "ask ai",
        "ask llm",
        "claude",
        "chatgpt",
        "ai help"
      ],
      "fields": {
        "prompt": {
          "type": "string",
          "required": true
        },
        "model": {
          "type": "string",
          "required": false,
          "default": "claude"
        }
      },
      "command": "echo 'LLM query'",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "ask claude how to set up nginx reverse proxy",
        "ask chatgpt explain iptables",
        "ask ai about load balancing"
      ]
    },
    {
      "name": "llm.claude_cli",
      "description": "Run a command through Claude CLI",
      "synonyms": [
        "claude cli",
        "run claude",
        "claude code",
        "use claude"
      ],
      "fields": {
        "prompt": {
          "type": "string",
          "required": true
        }
      },
      "command": "claude -p '{{prompt}}'",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "claude cli explain this error",
        "run claude to review my config"
      ]
    },
    {
      "name": "llm.convex",
      "description": "Run Convex CLI commands",
      "synonyms": [
        "convex",
        "convex cli",
        "convex deploy",
        "convex dev",
        "convex run"
      ],
      "fields": {
        "action": {
          "type": "string",
          "required": false,
          "default": "dev"
        }
      },
      "command": "npx convex {{action}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "convex dev",
        "convex deploy",
        "convex run"
      ]
    },

    {
      "name": "project.scan",
      "description": "Scan for software projects in a directory — detects type, version, deps, scripts",
      "synonyms": ["scan projects", "what projects", "find projects", "check what projects", "scan for projects", "check projects", "project scan", "what projects do i have", "list projects", "show projects", "my projects", "projects here", "what repos", "what apps"],
      "fields": {
        "path": { "type": "string", "required": false, "default": "." }
      },
      "command": "notoken:project-scan:{{path}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "scan for projects",
        "what projects are on here",
        "what projects do i have here",
        "check what projects are in this folder",
        "find projects",
        "list my projects",
        "show me my projects"
      ]
    },
    {
      "name": "dir.list",
      "description": "Show directory contents with file types, sizes, and project detection",
      "synonyms": ["where are my files", "what files", "what is here", "whats here", "whats in this folder", "whats in this directory", "show files", "list files", "show directory", "directory listing", "folder contents", "what do i have here", "show me whats here", "where is everything", "show this folder", "list directory", "list this folder", "what is in this folder", "what is in this directory", "show me this folder", "what have i got here", "what have i got"],
      "fields": {
        "path": { "type": "string", "required": false, "default": "." }
      },
      "command": "notoken:dir-list:{{path}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "where are my files",
        "what files do i have here",
        "what's in this folder",
        "show me what's here",
        "list files",
        "what do i have in this directory"
      ]
    },
    {
      "name": "project.info",
      "description": "Show info about the current project (type, deps, scripts)",
      "synonyms": ["project info", "what project is this", "what kind of project", "project type", "what framework", "what stack"],
      "fields": {
        "path": { "type": "string", "required": false, "default": "." }
      },
      "command": "echo '=== Project Info ===' && ls {{path}}/package.json 2>/dev/null && echo 'Node.js project' && cat {{path}}/package.json 2>/dev/null | head -20 || ls {{path}}/Cargo.toml 2>/dev/null && echo 'Rust project' || ls {{path}}/go.mod 2>/dev/null && echo 'Go project' || ls {{path}}/requirements.txt 2>/dev/null && echo 'Python project' || ls {{path}}/composer.json 2>/dev/null && echo 'PHP project' || echo 'Unknown project type'",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": [
        "what project is this",
        "project info",
        "what framework is this"
      ]
    },
    {
      "name": "openclaw.start",
      "description": "Start the OpenClaw gateway",
      "synonyms": ["start openclaw", "openclaw start", "start gateway", "openclaw gateway", "run openclaw"],
      "fields": {},
      "command": "openclaw gateway --verbose",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": ["start openclaw", "run the gateway", "openclaw start"]
    },
    {
      "name": "openclaw.stop",
      "description": "Stop the OpenClaw gateway",
      "synonyms": ["stop openclaw", "openclaw stop", "stop gateway", "kill openclaw"],
      "fields": {},
      "command": "pkill -f 'openclaw gateway' 2>/dev/null && echo 'Gateway stopped' || echo 'Gateway not running'",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": ["stop openclaw", "stop the gateway"]
    },
    {
      "name": "openclaw.restart",
      "description": "Restart the OpenClaw gateway",
      "synonyms": ["restart openclaw", "openclaw restart", "restart gateway", "bounce openclaw"],
      "fields": {},
      "command": "pkill -f 'openclaw gateway' 2>/dev/null; sleep 1; openclaw gateway --verbose &",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": ["restart openclaw", "restart the gateway"]
    },
    {
      "name": "openclaw.status",
      "description": "Check OpenClaw gateway status and channels",
      "synonyms": ["openclaw status", "openclaw check", "is openclaw running", "check openclaw", "gateway status"],
      "fields": {},
      "command": "echo '=== OpenClaw Status ===' && openclaw --version 2>&1 && echo '' && openclaw channels status 2>&1 && echo '' && openclaw channels list 2>&1 || echo 'OpenClaw not available'",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": ["openclaw status", "is openclaw running", "check openclaw"]
    },
    {
      "name": "openclaw.doctor",
      "description": "Run OpenClaw doctor to diagnose and fix issues",
      "synonyms": ["openclaw doctor", "fix openclaw", "diagnose openclaw", "openclaw fix"],
      "fields": {},
      "command": "openclaw doctor --fix 2>&1 || echo 'OpenClaw not available — run: notoken setup openclaw'",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": ["openclaw doctor", "fix openclaw", "diagnose openclaw"]
    },

    {
      "name": "browser.open",
      "description": "Open a URL in a browser",
      "synonyms": ["open", "browse", "navigate", "visit", "go to", "load", "launch browser", "open browser", "open url", "open website", "open page", "open site"],
      "fields": {
        "url": { "type": "string", "required": true }
      },
      "command": "notoken:browse:{{url}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": ["open google.com", "browse to localhost:3000", "visit example.com", "go to github.com", "navigate to notoken.sh"]
    },
    {
      "name": "browser.screenshot",
      "description": "Take a screenshot of a webpage",
      "synonyms": ["screenshot", "capture", "snap", "screencap", "screen grab", "take screenshot", "capture page", "save page"],
      "fields": {
        "url": { "type": "string", "required": true }
      },
      "command": "notoken:browse:screenshot:{{url}}",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": ["take screenshot of google.com", "screenshot example.com", "capture localhost:3000", "screencap notoken.sh"]
    },
    {
      "name": "browser.status",
      "description": "Show browser engine status",
      "synonyms": ["browser status", "browser engines", "check browser", "which browser", "browser info"],
      "fields": {},
      "command": "notoken:browse:status",
      "execution": "local",
      "requiresConfirmation": false,
      "riskLevel": "low",
      "examples": ["browser status", "check browser", "which browser engines", "show browser info"]
    },
    {
      "name": "browser.install",
      "description": "Install a browser engine for automation",
      "synonyms": ["install browser", "install patchright", "install playwright", "setup browser", "install chromium"],
      "fields": {
        "engine": { "type": "string", "required": false, "default": "patchright" }
      },
      "command": "notoken:browse:install:{{engine}}",
      "execution": "local",
      "requiresConfirmation": true,
      "riskLevel": "medium",
      "examples": ["install browser", "install patchright", "install playwright", "setup browser engine"]
    },


    {"name":"knowledge.lookup","description":"Look up a topic, concept, or entity on Wikidata","synonyms":["what is","who is","tell me about","define","lookup","look up","wiki","wikipedia","explain","info about","information about","facts about","learn about","whats a","what are"],"fields":{"topic":{"type":"string","required":true}},"command":"notoken:wiki-lookup:{{topic}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["what is kubernetes","who is linus torvalds","tell me about nginx","define websocket","what is docker compose","lookup react hooks"]},

    {"name":"ai.generate_image","description":"Generate an image from a text prompt using local Stable Diffusion or suggest online services","synonyms":["generate image","generate a picture","create image","create a picture","make an image","make a picture","draw","paint","generate art","ai image","create art","make me an image","make me a picture","draw me","paint me","generate photo","create a photo","imagine","generate a picture of","picture of","image of","draw a","paint a","create artwork"],"fields":{"prompt":{"type":"string","required":true}},"command":"notoken:image-gen:{{prompt}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["generate a picture of a cat","create an image of a sunset","draw a robot","make me a picture of a mountain","paint a landscape"]},

    {"name":"ai.image_status","description":"Check status of image generation engines — what we use to make images","synonyms":["image status","stable diffusion status","sd status","check image generator","image engine status","is stable diffusion running","check sd","what are we using to generate","what generates images","how are images generated","image generation status","what engine","which image generator","what model are we using","what are we using for images","how do we generate images"],"fields":{},"command":"notoken:image-status","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["image status","check stable diffusion","is sd running","image engine status"]},

    {"name":"ai.install_sd","description":"Install Stable Diffusion or other image generation software","synonyms":["install stable diffusion","install sd","install image generator","install comfyui","install fooocus","setup stable diffusion","setup image generation","install ai art","install diffusion"],"fields":{"engine":{"type":"string","required":false,"default":"auto1111"}},"command":"notoken:install-sd:{{engine}}","execution":"local","requiresConfirmation":true,"riskLevel":"medium","examples":["install stable diffusion","install comfyui","install fooocus","setup image generation"]},

    {"name":"ai.start_sd","description":"Start the Stable Diffusion web UI","synonyms":["start stable diffusion","start sd","launch sd","run stable diffusion","start image generator","start sd webui"],"fields":{},"command":"cd ~/stable-diffusion-webui && bash webui.sh --api --listen &","execution":"local","requiresConfirmation":true,"riskLevel":"low","examples":["start stable diffusion","launch sd","run the image generator"]},

    {"name":"ai.stop_sd","description":"Stop the Stable Diffusion web UI","synonyms":["stop stable diffusion","stop sd","kill sd","shutdown sd","stop image generator"],"fields":{},"command":"pkill -f 'webui.py\\|stable-diffusion' 2>/dev/null && echo 'Stable Diffusion stopped' || echo 'Not running'","execution":"local","requiresConfirmation":true,"riskLevel":"medium","examples":["stop stable diffusion","stop sd","kill the image generator"]},

    {"name":"dns.lookup","description":"Look up DNS records for a domain","synonyms":["dns lookup","check dns","nslookup","dig","dns records","check my dns","resolve domain","dns query","whats the dns for","dns for"],"fields":{"domain":{"type":"string","required":true},"type":{"type":"string","required":false,"default":"A"}},"command":"echo '=== DNS Records ===' && dig {{domain}} {{type}} +short && echo '' && echo '=== All Records ===' && dig {{domain}} ANY +noall +answer 2>/dev/null && echo '' && echo '=== Nameservers ===' && dig {{domain}} NS +short","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["check dns for google.com","nslookup notoken.sh","dig example.com","what are the dns records for notoken.sh"]},

    {"name":"dns.update","description":"Update a DNS record (via provider CLI or /etc/hosts)","synonyms":["update dns","change dns","add dns record","add A record","add CNAME","set dns","modify dns","point domain"],"fields":{"domain":{"type":"string","required":true},"type":{"type":"string","required":false,"default":"A"},"value":{"type":"string","required":true}},"command":"echo '{{domain}} → {{value}} ({{type}})' && echo 'To update /etc/hosts:' && echo '{{value}} {{domain}}' && echo '' && echo 'For cloud DNS, use your provider CLI:' && echo '  aws route53 change-resource-record-sets ...' && echo '  gcloud dns record-sets update ...' && echo '  doctl compute domain records create ...'","execution":"local","requiresConfirmation":true,"riskLevel":"high","examples":["add A record for api.example.com pointing to 1.2.3.4","update dns for mysite.com","point domain to new IP"]},

    {"name":"firewall.list","description":"Show firewall rules (ufw, iptables, firewalld)","synonyms":["firewall rules","show firewall","list firewall","iptables","ufw status","firewall status","check firewall","what ports are open","firewall list"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== UFW ===' && ufw status verbose 2>/dev/null || echo '(ufw not active)' && echo '' && echo '=== iptables ===' && iptables -L -n --line-numbers 2>/dev/null | head -30 || echo '(no iptables access)' && echo '' && echo '=== firewalld ===' && firewall-cmd --list-all 2>/dev/null || echo '(firewalld not active)'","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["show firewall rules","ufw status","what ports are open","check firewall"]},

    {"name":"firewall.open","description":"Open a port in the firewall","synonyms":["open port","allow port","ufw allow","firewall open","unblock port","permit port"],"fields":{"port":{"type":"string","required":true},"protocol":{"type":"string","required":false,"default":"tcp"},"environment":{"type":"environment","required":false,"default":"local"}},"command":"ufw allow {{port}}/{{protocol}} 2>/dev/null && ufw status || firewall-cmd --add-port={{port}}/{{protocol}} --permanent 2>/dev/null && firewall-cmd --reload || iptables -A INPUT -p {{protocol}} --dport {{port}} -j ACCEPT","execution":"local","requiresConfirmation":true,"riskLevel":"high","examples":["open port 8080","allow port 443","ufw allow 3000"]},

    {"name":"firewall.close","description":"Close/block a port in the firewall","synonyms":["close port","block port","ufw deny","firewall close","deny port","block ip"],"fields":{"port":{"type":"string","required":true},"protocol":{"type":"string","required":false,"default":"tcp"},"environment":{"type":"environment","required":false,"default":"local"}},"command":"ufw deny {{port}}/{{protocol}} 2>/dev/null && ufw status || firewall-cmd --remove-port={{port}}/{{protocol}} --permanent 2>/dev/null && firewall-cmd --reload || iptables -A INPUT -p {{protocol}} --dport {{port}} -j DROP","execution":"local","requiresConfirmation":true,"riskLevel":"high","examples":["close port 8080","block port 22","deny port 3306"]},

    {"name":"firewall.block_ip","description":"Block an IP address","synonyms":["block ip","ban ip","blacklist ip","deny ip","firewall block"],"fields":{"ip":{"type":"string","required":true},"environment":{"type":"environment","required":false,"default":"local"}},"command":"ufw deny from {{ip}} 2>/dev/null && ufw status || iptables -A INPUT -s {{ip}} -j DROP","execution":"local","requiresConfirmation":true,"riskLevel":"high","examples":["block ip 1.2.3.4","ban ip 10.0.0.5","blacklist 192.168.1.100"]},

    {"name":"systemd.create","description":"Create a systemd service unit for an application","synonyms":["create systemd service","make service","create service","run on boot","make this run on boot","autostart","systemd unit","create unit file","register service"],"fields":{"name":{"type":"string","required":true},"command":{"type":"string","required":true},"user":{"type":"string","required":false,"default":"root"},"workdir":{"type":"string","required":false}},"command":"cat > /etc/systemd/system/{{name}}.service << 'UNIT'\n[Unit]\nDescription={{name}} service\nAfter=network.target\n\n[Service]\nType=simple\nUser={{user}}\nWorkingDirectory={{workdir}}\nExecStart={{command}}\nRestart=on-failure\nRestartSec=5\n\n[Install]\nWantedBy=multi-user.target\nUNIT\nsystemctl daemon-reload && systemctl enable {{name}} && systemctl start {{name}} && echo 'Service created and started' && systemctl status {{name}} --no-pager","execution":"local","requiresConfirmation":true,"riskLevel":"high","examples":["create systemd service for my node app","make my api run on boot","register service myapp"]},

    {"name":"package.audit","description":"Check for outdated or vulnerable packages","synonyms":["package audit","npm audit","check vulnerabilities","outdated packages","security audit","vulnerable packages","check outdated","what packages are outdated","check for vulnerabilities","dependency check"],"fields":{"manager":{"type":"string","required":false,"default":"auto"},"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Outdated Packages ===' && (npm outdated 2>/dev/null || pip list --outdated 2>/dev/null || echo '(no package manager detected)') && echo '' && echo '=== Security Audit ===' && (npm audit 2>/dev/null || pip-audit 2>/dev/null || echo '(no audit tool available)')","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["npm audit","check for vulnerabilities","what packages are outdated","security audit"]},

    {"name":"package.update_all","description":"Update all packages to latest versions","synonyms":["update all packages","upgrade packages","npm update","pip upgrade","update dependencies","upgrade all","update everything"],"fields":{"manager":{"type":"string","required":false,"default":"auto"},"environment":{"type":"environment","required":false,"default":"local"}},"command":"if [ -f package.json ]; then npm update && echo 'npm packages updated'; elif [ -f requirements.txt ]; then pip install --upgrade -r requirements.txt && echo 'pip packages updated'; elif [ -f Gemfile ]; then bundle update && echo 'gems updated'; else echo 'No package manager detected'; fi","execution":"local","requiresConfirmation":true,"riskLevel":"medium","examples":["update all packages","npm update","upgrade all dependencies"]},

    {"name":"runtime.install","description":"Install or switch to a specific runtime version","synonyms":["install node","install python","install java","switch to node","set up node","install runtime","use node","use python","set up python","install go","install rust","switch node version","switch python version"],"fields":{"runtime":{"type":"string","required":true},"version":{"type":"string","required":false}},"command":"echo 'Installing {{runtime}} {{version}}...' && if echo '{{runtime}}' | grep -qi 'node'; then nvm install {{version}} 2>/dev/null && nvm use {{version}} || echo 'Install nvm first: notoken fix nvm'; elif echo '{{runtime}}' | grep -qi 'python'; then pyenv install {{version}} 2>/dev/null && pyenv global {{version}} || apt-get install -y python{{version}} 2>/dev/null || echo 'Use pyenv or apt'; elif echo '{{runtime}}' | grep -qi 'go'; then curl -sSL https://go.dev/dl/go{{version}}.linux-amd64.tar.gz | tar -C /usr/local -xz; elif echo '{{runtime}}' | grep -qi 'rust'; then curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y; else echo 'Unknown runtime: {{runtime}}'; fi","execution":"local","requiresConfirmation":true,"riskLevel":"medium","examples":["install node 22","set up python 3.12","switch to node 20","install go 1.22","install rust"]},

    {"name":"cloud.aws","description":"Run AWS CLI commands","synonyms":["aws","aws s3","aws ec2","aws lambda","s3 list","s3 ls","aws iam","check aws","aws status"],"fields":{"action":{"type":"string","required":true},"resource":{"type":"string","required":false}},"command":"aws {{action}} {{resource}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["aws s3 ls","aws ec2 describe-instances","aws lambda list-functions","check aws s3 buckets"]},

    {"name":"cloud.gcloud","description":"Run Google Cloud CLI commands","synonyms":["gcloud","gcp","google cloud","gcloud compute","gcloud run","gcloud functions"],"fields":{"action":{"type":"string","required":true},"resource":{"type":"string","required":false}},"command":"gcloud {{action}} {{resource}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["gcloud compute instances list","gcloud run services list","gcloud functions list"]},

    {"name":"cloud.az","description":"Run Azure CLI commands","synonyms":["az","azure","az vm","az webapp","az storage","azure cli"],"fields":{"action":{"type":"string","required":true},"resource":{"type":"string","required":false}},"command":"az {{action}} {{resource}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["az vm list","az webapp list","az storage account list"]},

    {"name":"cloud.do","description":"Run DigitalOcean CLI commands","synonyms":["doctl","digitalocean","do droplets","do spaces"],"fields":{"action":{"type":"string","required":true},"resource":{"type":"string","required":false}},"command":"doctl {{action}} {{resource}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["doctl compute droplet list","doctl apps list"]},

    {"name":"monitor.watch_log","description":"Watch a log file in real-time with optional filter","synonyms":["watch log","monitor log","tail log","follow log","watch for errors","monitor for","alert me if","stream log"],"fields":{"path":{"type":"string","required":true},"filter":{"type":"string","required":false},"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo 'Watching {{path}}...' && if [ -n '{{filter}}' ] && [ '{{filter}}' != '{{filter}}' ]; then tail -f {{path}} | grep --line-buffered '{{filter}}'; else tail -f {{path}}; fi","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["watch /var/log/nginx/error.log","monitor log for errors","tail syslog","follow /var/log/app.log"]},

    {"name":"monitor.disk_alert","description":"Check if disk usage exceeds a threshold","synonyms":["alert disk","disk threshold","warn if disk","check disk threshold","monitor disk","disk alert"],"fields":{"threshold":{"type":"string","required":false,"default":"80"},"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Disk Usage Check (threshold: {{threshold}}%) ===' && df -h --output=target,pcent,size,avail | tail -n +2 | while read mount pct size avail; do num=$(echo $pct | tr -d '%'); if [ \"$num\" -ge {{threshold}} ] 2>/dev/null; then echo \"⚠ $mount: $pct used ($avail free of $size)\"; fi; done && echo '' && echo '=== All mounts ===' && df -h | head -15","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["alert me if disk hits 90%","check disk threshold 80","monitor disk usage"]},

    {"name":"monitor.process","description":"Monitor a specific process — CPU, memory, uptime","synonyms":["monitor process","watch process","check process","process info","what is using cpu","what is eating memory","top process","ps aux","process status"],"fields":{"name":{"type":"string","required":false},"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Top by CPU ===' && ps aux --sort=-%cpu | head -10 && echo '' && echo '=== Top by Memory ===' && ps aux --sort=-%mem | head -10 && echo '' && echo '=== Process Tree ===' && pstree -p 2>/dev/null | head -20 || ps -ef --forest | head -20","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["what is using cpu","what is eating memory","check process nginx","top processes"]},

    {"name":"network.traceroute","description":"Trace the network path to a host","synonyms":["traceroute","tracepath","trace route","network path","hops to","mtr","trace"],"fields":{"target":{"type":"string","required":true},"environment":{"type":"environment","required":false,"default":"local"}},"command":"traceroute -m 20 {{target}} 2>/dev/null || tracepath {{target}} 2>/dev/null || echo 'traceroute not installed — install with: apt install traceroute'","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["traceroute to google.com","trace route to 8.8.8.8","network path to prod"]},

    {"name":"network.dig","description":"Detailed DNS query with dig","synonyms":["dig domain","dns dig","detailed dns","reverse dns","reverse lookup","ptr record"],"fields":{"target":{"type":"string","required":true},"type":{"type":"string","required":false,"default":"A"}},"command":"dig {{target}} {{type}} +noall +answer +comments && echo '' && echo '=== Reverse DNS ===' && dig -x {{target}} +short 2>/dev/null || echo '(not an IP)'","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["dig google.com","reverse dns 8.8.8.8","dig notoken.sh MX"]},

    {"name":"network.speedtest","description":"Test network speed and latency","synonyms":["speedtest","network speed","bandwidth test","internet speed","how fast is my connection","speed test","check speed"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"if command -v speedtest-cli &>/dev/null; then speedtest-cli --simple; elif command -v curl &>/dev/null; then echo 'Download speed:' && curl -s -o /dev/null -w 'Speed: %{speed_download} bytes/sec\\nTime: %{time_total}s\\n' https://speed.cloudflare.com/__down?bytes=10000000 && echo '' && echo 'Latency:' && ping -c 3 1.1.1.1 | tail -1; else echo 'Install speedtest-cli: pip install speedtest-cli'; fi","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["speedtest","check internet speed","how fast is my connection","bandwidth test"]},

    {"name":"network.connections","description":"Show active network connections","synonyms":["active connections","netstat","ss","who is connected","network connections","established connections","open connections","tcp connections"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Listening Ports ===' && ss -tlnp 2>/dev/null | head -20 || netstat -tlnp 2>/dev/null | head -20 && echo '' && echo '=== Established Connections ===' && ss -tnp state established 2>/dev/null | head -20 || netstat -tnp 2>/dev/null | grep ESTABLISHED | head -20 && echo '' && echo '=== Connection Count by State ===' && ss -s 2>/dev/null || netstat -s 2>/dev/null | head -15","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["show active connections","who is connected","netstat","list open connections"]},

    {"name":"text.count","description":"Count lines, words, or characters in a file","synonyms":["count lines","count words","line count","word count","wc","how many lines","how big is this file","file size","count characters"],"fields":{"path":{"type":"string","required":true},"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== File Stats ===' && wc -l -w -c {{path}} | awk '{print \"Lines: \" $1 \"\\nWords: \" $2 \"\\nBytes: \" $3 \"\\nFile:  \" $4}' && echo '' && ls -lh {{path}} | awk '{print \"Size: \" $5}' && echo '' && echo '=== First 5 lines ===' && head -5 {{path}} && echo '' && echo '=== Last 5 lines ===' && tail -5 {{path}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["count lines in /var/log/syslog","how many lines in package.json","wc my-file.txt"]},

    {"name":"text.sort","description":"Sort a file's contents","synonyms":["sort file","sort lines","sort this file","alphabetize","sort by","order file"],"fields":{"path":{"type":"string","required":true},"flags":{"type":"string","required":false,"default":""},"environment":{"type":"environment","required":false,"default":"local"}},"command":"sort {{flags}} {{path}}","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["sort file /tmp/list.txt","sort this file alphabetically","sort -n numbers.txt"]},

    {"name":"text.unique","description":"Remove duplicate lines from a file","synonyms":["remove duplicates","unique lines","deduplicate","dedup","uniq","distinct lines"],"fields":{"path":{"type":"string","required":true},"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Unique lines ===' && sort {{path}} | uniq && echo '' && echo '=== Duplicate summary ===' && sort {{path}} | uniq -c | sort -rn | head -10","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["remove duplicates from list.txt","unique lines in hosts.txt","dedup /tmp/ips.txt"]},

    {"name":"text.diff","description":"Compare two files and show differences","synonyms":["diff files","compare files","difference between","what changed","diff","compare"],"fields":{"file1":{"type":"string","required":true},"file2":{"type":"string","required":true},"environment":{"type":"environment","required":false,"default":"local"}},"command":"diff --color {{file1}} {{file2}} || echo '(files differ)'","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["diff config.old config.new","compare /etc/nginx/nginx.conf /tmp/nginx.conf.bak"]},

    {"name":"text.replace","description":"Find and replace text in a file","synonyms":["find and replace","replace text","sed replace","search and replace","substitute","change text in file"],"fields":{"find":{"type":"string","required":true},"replace":{"type":"string","required":true},"path":{"type":"string","required":true},"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo 'Replacing \"{{find}}\" with \"{{replace}}\" in {{path}}' && sed -i 's/{{find}}/{{replace}}/g' {{path}} && echo 'Done. Preview:' && grep -n '{{replace}}' {{path}} | head -5","execution":"local","requiresConfirmation":true,"riskLevel":"medium","examples":["replace localhost with 0.0.0.0 in config.js","find and replace foo with bar in settings.json"]},

    {"name":"clipboard.copy","description":"Copy text or file contents to clipboard","synonyms":["copy to clipboard","clipboard","copy output","copy that","yank","pbcopy","xclip"],"fields":{"source":{"type":"string","required":false}},"command":"if [ -n '{{source}}' ]; then cat {{source}} | (xclip -selection clipboard 2>/dev/null || pbcopy 2>/dev/null || clip.exe 2>/dev/null || echo 'No clipboard tool found'); echo 'Copied to clipboard'; else echo 'Pipe output to: | xclip -sel clip (Linux) or | pbcopy (macOS) or | clip.exe (WSL)'; fi","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["copy to clipboard","copy output of last command","copy /etc/hosts to clipboard"]},

    {"name":"output.save","description":"Save command output to a file","synonyms":["save output","save to file","redirect to file","output to file","write output","export output","pipe to file"],"fields":{"command":{"type":"string","required":true},"path":{"type":"string","required":true},"environment":{"type":"environment","required":false,"default":"local"}},"command":"{{command}} > {{path}} 2>&1 && echo 'Output saved to {{path}} ('$(wc -l < {{path}})' lines)'","execution":"local","requiresConfirmation":true,"riskLevel":"low","examples":["save output to /tmp/results.txt","redirect to file /root/log.txt"]},

    {"name":"system.hostname","description":"Show or set the system hostname","synonyms":["hostname","what is my hostname","system name","machine name","set hostname","change hostname"],"fields":{"name":{"type":"string","required":false},"environment":{"type":"environment","required":false,"default":"local"}},"command":"if [ -n '{{name}}' ] && [ '{{name}}' != '{{name}}' ]; then hostnamectl set-hostname {{name}} && echo 'Hostname set to {{name}}'; else hostname && echo '' && hostnamectl 2>/dev/null; fi","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["what is my hostname","hostname","set hostname to webserver-01"]},

    {"name":"system.timezone","description":"Show or set the system timezone","synonyms":["timezone","what timezone","check timezone","set timezone","change timezone","timedatectl","current time"],"fields":{"tz":{"type":"string","required":false},"environment":{"type":"environment","required":false,"default":"local"}},"command":"if [ -n '{{tz}}' ] && [ '{{tz}}' != '{{tz}}' ]; then timedatectl set-timezone {{tz}} && echo 'Timezone set to {{tz}}'; else timedatectl 2>/dev/null || (date && cat /etc/timezone 2>/dev/null); fi","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["what timezone am I in","check timezone","set timezone to America/New_York"]},

    {"name":"system.reboot_history","description":"Show system reboot history","synonyms":["reboot history","when was last reboot","boot history","restart history","last reboot","system restarts"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Reboot History ===' && last reboot | head -15 && echo '' && echo '=== Last Shutdown ===' && last -x shutdown | head -5 2>/dev/null","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["when was the last reboot","reboot history","show restart history"]},

    {"name":"system.kernel","description":"Show kernel and OS information","synonyms":["kernel version","kernel info","os version","linux version","uname","what os","what linux","distro info","release info"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Kernel ===' && uname -a && echo '' && echo '=== OS Release ===' && cat /etc/os-release 2>/dev/null && echo '' && echo '=== Hardware ===' && lscpu 2>/dev/null | head -10","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["kernel version","what os am I running","uname","what linux distro"]},

    {"name":"system.env","description":"Show environment variables","synonyms":["environment variables","env vars","show env","printenv","list env","what are my env vars","PATH","show PATH"],"fields":{"filter":{"type":"string","required":false},"environment":{"type":"environment","required":false,"default":"local"}},"command":"if [ -n '{{filter}}' ] && [ '{{filter}}' != '{{filter}}' ]; then env | grep -i '{{filter}}' | sort; else env | sort | head -50 && echo '' && echo '(showing first 50 — use filter to narrow)'; fi","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["show env vars","printenv","show PATH","env vars matching NODE"]},

    {"name":"system.resource_summary","description":"Full system resource summary — CPU, memory, disk, load, processes","synonyms":["system summary","resource summary","system resources","how is my system","system overview","machine status","server overview","full system check","system report"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== System ===' && uname -n && uptime && echo '' && echo '=== CPU ===' && nproc && cat /proc/loadavg && echo '' && echo '=== Memory ===' && free -h && echo '' && echo '=== Disk ===' && df -h --total | tail -5 && echo '' && echo '=== Top Processes ===' && ps aux --sort=-%cpu | head -6 && echo '' && echo '=== Network ===' && ip addr show 2>/dev/null | grep 'inet ' | head -5 || ifconfig 2>/dev/null | grep 'inet ' | head -5","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["system summary","how is my system","full system check","server overview","resource summary"]},

    {"name":"network.ip","description":"Show IP addresses and network interfaces","synonyms":["my ip","ip address","what is my ip","show ip","network interfaces","ifconfig","ip addr","public ip","external ip","local ip"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Local IPs ===' && ip addr show 2>/dev/null | grep 'inet ' || ifconfig 2>/dev/null | grep 'inet ' && echo '' && echo '=== Public IP ===' && curl -sf --max-time 3 https://api.ipify.org && echo '' || echo '(could not determine)'","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["what is my ip","show ip address","my public ip","network interfaces"]},

    {"name":"network.bandwidth","description":"Show network bandwidth usage per interface","synonyms":["bandwidth usage","network usage","traffic","network traffic","interface stats","bytes sent","bytes received","network stats"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== Interface Statistics ===' && cat /proc/net/dev 2>/dev/null | awk 'NR>2{print $1, \"RX:\", $2, \"TX:\", $10}' || ifconfig 2>/dev/null && echo '' && echo '=== Active Connections ===' && ss -s 2>/dev/null","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["network bandwidth","traffic stats","bytes sent and received","network usage"]},

    {"name":"hardware.info","description":"Show hardware information — CPU, RAM, GPU, storage","synonyms":["hardware info","cpu info","ram info","gpu info","lshw","lspci","what hardware","machine specs","system specs","how much ram","how many cores","cpu model"],"fields":{"environment":{"type":"environment","required":false,"default":"local"}},"command":"echo '=== CPU ===' && lscpu 2>/dev/null | head -15 || cat /proc/cpuinfo | head -20 && echo '' && echo '=== Memory ===' && free -h && echo '' && echo '=== Storage ===' && lsblk 2>/dev/null | head -15 || fdisk -l 2>/dev/null | head -15 && echo '' && echo '=== GPU ===' && lspci 2>/dev/null | grep -i vga || echo '(no GPU detected)'","execution":"local","requiresConfirmation":false,"riskLevel":"low","examples":["hardware info","how much ram do I have","cpu info","how many cores","machine specs"]}

  ]
}

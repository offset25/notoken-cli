{
  "name": "notoken-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=packages/core",
    "dev": "npm run dev --workspace=packages/cli"
  },
  "version": "1.6.1",
  "dependencies": {
    "patchright": "^1.59.1",
    "playwright": "^1.59.1"
  }
}
